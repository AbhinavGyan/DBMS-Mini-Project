    Patient's Login and signup ->

    1. (creating person signup table) 
        create table person(personID int(255) not null primary key auto_increment , firstName varchar(150) not null , middleName varchar(150) , 
        lastName varchar(150) not null, dob date not null ,gender varchar(10) not null, phoneNumber varchar(10) not null);

    2. (creating email and pass table of person having password encrypted by md5)
        create table personLogin(personID int(255) not null auto_increment,email varchar(100) not null ,password varchar(150) not null,foreign key (personID) references 
        person(personID));

    3. (inserting from sign up page to person table using wel.php)
        insert into person(firstName,middleName,lastName,dob,gender,phoneNumber)
    	values('$x','$y','$z','$dob','$gender','$mobile');

    4. (inserting from signup page to personLogin table using wel.php with password encrypted with md5)
        insert into personLogin(email,password)
        values('$mail','$password');

    5. (checking in Log.php the correct values of email and password to get login from table pogin)
        select * from personLogin where email='$n' and password='$password';

    6. (If logged checking personLogin and person table if the personID matches in both of them according to email and password)
        select person.* from personLogin , person where person.pid = personLogin.pid and personLogin.email = '$email' and personLogin.pass = '$password';

    Doctor's Login and signup ->

    7. (creating doctor's signup table)
        create table doctor(doctorID int(255) not null primary key auto_increment , firstName varchar(150) not null , middleName varchar(150),lastName varchar(150) 
        not null,dob date not null ,gender varchar(10) not null, phoneNumber varchar(10) not null, qualification varchar(50) not null,department varchar(20) not null);

    8. (creating doctor's login table)
        create table doctorLogin(doctorID int(255) not null auto_increment,email varchar(100) not null ,password varchar(150) not null,foreign key (doctorID) references
        doctor(doctorID));

    9. (inserting from sign up page to doctor table using doctorWel.php)
        insert into doctor(firstName,middleName,lastName,dob,gender,phoneNumber,qualification,department)
    	values('$x','$y','$z','$dob','$gender','$mobile','$qual','$dept');

    10. (inserting from signup page to doctorLogin table using doctorWel.php with password encrypted with md5)
        insert into doctorLogin(email,password)
         values('$mail','$password');

    11. (checking in doctorLog.php the correct values of email and password to get login from table doctorLogin)
        select * from doctorLogin where email='$n' and password='$password';

    12. (If logged checking doctorLogin and doctor table if the doctorID matches in both of them according to email and password)
        select doctor.* from doctorLogin , doctor where doctor.doctorID = doctorLogin.doctorID and doctorLogin.email = '$email' and doctorLogin.password = '$password';

